{
  "name": "vortex-stream",
  "version": "0.1.0-beta.1",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "MIT",

  "scripts": {
    "build": "napi build --platform",
    "prepublishOnly": "npm run build"
  },

  "napi": {
    "name": "vortex-stream"
  },

  "devDependencies": {
    "@napi-rs/cli": "^3.0.0"
  }
}